# Benchmarks

Composition vs `prettier-plugin-tailwindcss` alone.

Scenarios: no variants, string-only, flat arrays, nested + compounds, multi-call, real-card, Vue `<script setup>`.

```bash
pnpm benchmark
pnpm benchmark:quick
```
